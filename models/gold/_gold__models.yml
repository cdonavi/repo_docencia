version: 2

models:
  - name: fct_orders
    description: Hechos de pedidos (capa Gold).
    columns:
      - name: order_id
        description: Clave primaria del hecho.
        data_tests:
          - unique:
              config:              
                severity: warn
                store_failures: true
                error_if: ">100"
                warn_if: ">0"
          - not_null:
              config:
                severity: error